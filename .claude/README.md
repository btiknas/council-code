# .claude/

Claude Code project config lives here when users run the plugin in project-scoped mode.

Currently intentionally empty — the plugin ships as a marketplace plugin and installs into the user's global `~/.claude/` directory.

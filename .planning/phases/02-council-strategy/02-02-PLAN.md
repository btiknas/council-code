---
phase: 02-council-strategy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/council-strategy/SKILL.md
autonomous: true
requirements:
  - STRT-03
  - STRT-04

must_haves:
  truths:
    - "User can invoke /council-strategy and trigger the strategy council orchestrator"
    - "Chairman synthesis uses strategy-specific axes: Strategic Question, Viability Assessment, Market Fit Analysis, Risk/Reward Matrix, Competitive Position, Verdict"
    - "Verdict is one of exactly three values: Go, No-Go, or Pivot"
    - "5 advisors are spawned in parallel in a single message (not sequentially)"
  artifacts:
    - path: "skills/council-strategy/SKILL.md"
      provides: "Strategy council orchestrator with custom synthesis"
      contains: "disable-model-invocation: true"
      exports: ["council-strategy skill"]
  key_links:
    - from: "skills/council-strategy/SKILL.md"
      to: "agents/strategy-*.md"
      via: "Task tool parallel spawn referencing agent files"
      pattern: "agents/strategy-"
    - from: "skills/council-strategy/SKILL.md"
      to: "Claude Code skill runtime"
      via: "name + description frontmatter"
      pattern: "name: council-strategy"
---

<objective>
Write the council-strategy SKILL.md orchestrator with fully custom strategy synthesis structure and three-value categorical verdict.

Purpose: Delivers the orchestration layer that spawns 5 strategy advisors in parallel and synthesizes their output using strategy-specific success criteria (STRT-03). The synthesis structure (D-06) and verdict format (D-07) are the most critical differentiators from the code council — this SKILL.md must NOT reuse the code council's synthesis shape.

Output: skills/council-strategy/SKILL.md — the entry point for `/council-strategy`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-council-strategy/02-CONTEXT.md
@.planning/phases/02-council-strategy/02-RESEARCH.md
@.planning/phases/02-council-strategy/02-PATTERNS.md

<interfaces>
<!-- The SKILL.md for council-code is the structural template. The strategy SKILL.md replicates the STRUCTURE but rewrites the SYNTHESIS from scratch. -->

From skills/council-code/SKILL.md (frontmatter — replicate with strategy values):
```yaml
---
name: council-code
description: Multi-perspective code decision council. Runs 5 expert advisors (Contrarian, First Principles, Expansionist, Outsider, Executor) in parallel on a code/architecture/engineering question, then synthesizes a chairman verdict with agreements, clashes, blind spots, and a concrete next step.
disable-model-invocation: true
---
```

From skills/council-code/SKILL.md (Step 2 parallel spawn — replicate verbatim except agent names):
```markdown
### Step 2 — Spawn the 5 advisors in parallel

Use the Task tool to launch all 5 advisors **in a single message with 5 parallel tool calls**. Each gets the same brief:

Decision prompt: <one sentence>
Context: <relevant files, constraints, success criteria>
Your role: <see agents/<role>.md for full instructions>

Produce your analysis in the format specified by your role definition.
Do not read other advisors' output. Do not try to reach consensus.
Your job is to give the strongest possible version of your specific lens.

Use `subagent_type: general-purpose` for each. Give each a distinct `description` like "Contrarian review of billing split."
```

From skills/council-code/SKILL.md (Step 3 — DO NOT replicate. Rewrite from scratch per D-06):
```markdown
### Step 3 — Chairman synthesis
## Decision / ## Where the council agrees / ## Where the council clashes / ## Blind spots the council caught / ## Recommendation / ## The one thing to do first
```

REPLACE the above with D-06 custom axes:
```markdown
## Strategic Question / ## Viability Assessment / ## Market Fit Analysis / ## Risk/Reward Matrix / ## Competitive Position / ## Verdict: Go / No-Go / Pivot / ## First Move
```

From skills/council-code/SKILL.md (Step 4 follow-up offer — adapt for strategy domain):
```markdown
End with: "Want me to (a) execute the Executor's first action, (b) dig deeper into one advisor's view, or (c) re-run the council with additional context?"
```

From skills/council-code/SKILL.md (Guardrails — adapt for strategy domain):
```markdown
## Guardrails
- Do not let advisors anchor on each other.
- Do not summarize the advisors away.
- Resist false consensus.
- The Executor always gets the last word on action.
- Cost awareness.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the council-strategy SKILL.md orchestrator</name>
  <files>skills/council-strategy/SKILL.md</files>
  <read_first>
    - skills/council-code/SKILL.md (structural template — replicate structure, NOT synthesis content)
    - .planning/phases/02-council-strategy/02-CONTEXT.md (D-06 synthesis axes, D-07 verdict format, D-08 trigger phrases, D-10 non-overlapping triggers)
    - .planning/phases/02-council-strategy/02-RESEARCH.md (Chairman Synthesis Structure section with full template, Common Pitfalls 2 and 3)
  </read_first>
  <action>
Create the directory `skills/council-strategy/` and file `skills/council-strategy/SKILL.md`.

**Frontmatter** (per D-08):
```yaml
---
name: council-strategy
description: Multi-perspective strategy decision council. Runs 5 advisors (Devil's Advocate, Visionary, Pragmatist, Customer Champion, Operator) in parallel on a business or product decision, then synthesizes a chairman verdict with viability assessment, market fit analysis, risk/reward matrix, competitive position, and a Go/No-Go/Pivot call. Trigger phrases: strategy council, evaluate this business decision, should we pivot, pricing strategy, market analysis, business strategy.
disable-model-invocation: true
---
```

CRITICAL per D-10: The description MUST NOT contain: "stress test", "second opinion on code", "code review", "engineering decision", "what am I missing". These belong to the code council. Only strategy-specific triggers.

**Body structure** (follow council-code SKILL.md section ordering):

1. **H1 title**: `# council-strategy`
2. **Opening paragraph**: "Stress-test a business or product decision by spawning five independent strategy advisors in parallel, each with a distinct lens, then producing a single synthesized verdict with a Go/No-Go/Pivot call."

3. **## When to use this skill**: Invoke when the user asks to evaluate a business decision, pricing strategy, market entry, product roadmap pivot, partnership evaluation, or competitive positioning question. List use cases:
   - Pricing and monetization decisions
   - Market entry or expansion strategies
   - Product roadmap prioritization
   - Partnership or acquisition evaluation
   - Competitive positioning choices
   - Go-to-market strategy
   - Resource allocation across initiatives
   
   Trigger phrases (per D-08): `/council-strategy`, "strategy council", "evaluate this business decision", "should we pivot", "pricing strategy", "market analysis", "business strategy"

4. **## When NOT to use**: Adapt from code council pattern for strategy domain:
   - Simple factual market questions ("what's the market size for X") -- just answer directly.
   - Tiny operational decisions (meeting scheduling, minor budget reallocation) -- the council is overkill.
   - The user already has a decision and just needs help executing it -- point them to the Operator advisor directly.

5. **## The 5 Advisors**: Table with strategy roster (per D-01):

   | Advisor | Job |
   |---------|-----|
   | **Devil's Advocate** | Finds why this strategy fails |
   | **Visionary** | Finds the 10x opportunity the proposal undersells |
   | **Pragmatist** | Anchors to what's achievable given real constraints |
   | **Customer Champion** | Represents the buyer's actual decision process |
   | **Operator** | Turns the decision into a concrete execution plan |

   Agent definitions: `agents/strategy-devils-advocate.md`, etc.

6. **## Protocol**:

   **### Step 1 -- Extract the decision**: Same pattern as code council but with strategy framing:
   - **Decision prompt** -- one sentence
   - **Relevant context** -- the minimum market data, financial projections, competitive landscape, or business constraints the advisors need. Gather by reading/grepping, don't guess.
   - **Success criteria** -- what "good" looks like for the decision (revenue target, market share, timeline)
   
   If any unclear, ASK before proceeding.

   **### Step 2 -- Spawn the 5 advisors in parallel**: Replicate the code council Step 2 pattern EXACTLY, substituting strategy advisor names:
   ```
   Use the Task tool to launch all 5 advisors **in a single message with 5 parallel tool calls**. Each gets the same brief:

   Decision prompt: <one sentence>
   Context: <relevant files, constraints, success criteria>
   Your role: <see agents/<role>.md for full instructions>

   Produce your analysis in the format specified by your role definition.
   Do not read other advisors' output. Do not try to reach consensus.
   Your job is to give the strongest possible version of your specific lens.
   ```
   Use `subagent_type: general-purpose`. Distinct descriptions like "Devil's Advocate assessment of pricing strategy."

   **### Step 3 -- Chairman synthesis** (REWRITE FROM SCRATCH per D-06, D-07):
   
   DO NOT copy code council Step 3. Write this exact structure:
   ```
   ## Strategic Question
   [The decision, restated in one sentence]

   ## Viability Assessment
   [Can this strategy work given market, resources, and timing? Synthesize from all advisor inputs. Name which advisor(s) surfaced key evidence.]

   ## Market Fit Analysis
   [Does this address a real customer need with real willingness to pay? Primarily from Customer Champion + Devil's Advocate inputs.]

   ## Risk/Reward Matrix
   **Top Risks:** (ranked likelihood x impact)
   - [risk 1]
   - [risk 2]
   - [risk 3]

   **Top Rewards:** (ranked magnitude x probability)
   - [reward 1]
   - [reward 2]

   ## Competitive Position
   [Where does this land the company vs. competitors in 12-24 months? From Visionary + Devil's Advocate inputs.]

   ## Verdict: [Go / No-Go / Pivot]
   [One word first. Then: if Go -- conditions that must hold; if No-Go -- what would change it; if Pivot -- what specifically to pivot to.]

   ## First Move
   [Concrete next action with owner, deadline, and observable outcome. From Operator's output.]
   ```

   CRITICAL per D-07: Include explicit instruction: "The verdict is a single word: Go, No-Go, or Pivot. Write the word first, then conditions. Do not hedge. Do not present multiple verdicts as options."

   **### Step 4 -- Offer follow-up**: End with: "Want me to (a) start executing the Operator's first action, (b) dig deeper into one advisor's view, or (c) re-run the council with additional context?"

7. **## Guardrails** (adapt from code council for strategy domain):
   - **Do not let advisors anchor on each other.** Spawn all 5 in one parallel batch. Never sequential -- that contaminates the later advisors.
   - **Do not summarize the advisors away.** The synthesis section names which advisor said what. The user should be able to see which lens produced which point.
   - **Resist false consensus.** If 4 advisors agree and 1 dissents, the dissent often matters more than the agreement. Surface it explicitly.
   - **The Operator always gets the last word on action.** The synthesis's "First Move" must come from the Operator's output (or explicitly override with reasoning).
   - **The verdict is categorical.** Go, No-Go, or Pivot. Not "Go if X but No-Go if Y." Pick one and state conditions afterward.
   - **Cost awareness.** A full council is 5 subagent runs plus synthesis. Don't invoke for simple market questions.

8. **## References**: List all 5 strategy agent files with one-line descriptions:
   - `agents/strategy-devils-advocate.md` -- Assumption challenger
   - `agents/strategy-visionary.md` -- Long-horizon opportunity finder
   - `agents/strategy-pragmatist.md` -- Resource reality checker
   - `agents/strategy-customer-champion.md` -- Buyer perspective advocate
   - `agents/strategy-operator.md` -- Execution planner
  </action>
  <verify>
    <automated>cd /Users/D052192/src/council-code && test -f skills/council-strategy/SKILL.md && echo "file exists" && grep "name: council-strategy" skills/council-strategy/SKILL.md && grep "disable-model-invocation: true" skills/council-strategy/SKILL.md && grep "Viability Assessment" skills/council-strategy/SKILL.md && grep "Market Fit Analysis" skills/council-strategy/SKILL.md && grep "Risk/Reward Matrix" skills/council-strategy/SKILL.md && grep "Competitive Position" skills/council-strategy/SKILL.md && grep -E "Go / No-Go / Pivot|Go, No-Go, or Pivot" skills/council-strategy/SKILL.md && grep "First Move" skills/council-strategy/SKILL.md && echo "--- checking D-06 axes present ---" && echo "--- checking no code council synthesis leaked ---" && grep -c "Where the council agrees" skills/council-strategy/SKILL.md; test $? -eq 1 && echo "PASS: no code council synthesis structure" && echo "--- checking no overlapping triggers ---" && grep -c "stress test" skills/council-strategy/SKILL.md; test $? -eq 1 && echo "PASS: no overlapping triggers with code council"</automated>
  </verify>
  <acceptance_criteria>
    - skills/council-strategy/SKILL.md exists
    - File contains `name: council-strategy` in frontmatter
    - File contains `disable-model-invocation: true` in frontmatter
    - File contains `description:` with "strategy council" and "Go/No-Go/Pivot"
    - Description does NOT contain "stress test" or "second opinion on code" or "what am I missing"
    - File contains `## Strategic Question` section in Step 3
    - File contains `## Viability Assessment` section in Step 3
    - File contains `## Market Fit Analysis` section in Step 3
    - File contains `## Risk/Reward Matrix` section in Step 3
    - File contains `## Competitive Position` section in Step 3
    - File contains `## Verdict:` with Go / No-Go / Pivot
    - File contains `## First Move` section in Step 3
    - File does NOT contain `## Where the council agrees` (code council structure)
    - File does NOT contain `## Where the council clashes` (code council structure)
    - File does NOT contain `## Blind spots the council caught` (code council structure)
    - File contains "Do not read other advisors' output" in Step 2
    - File contains "subagent_type: general-purpose" or "subagent_type" reference in Step 2
    - File contains `## When to use this skill` and `## When NOT to use` sections
    - File contains `## Guardrails` section with parallel-spawn and categorical verdict rules
    - File contains `## References` section listing all 5 strategy agent files
  </acceptance_criteria>
  <done>Strategy SKILL.md orchestrator created with D-06 custom synthesis axes, D-07 three-value verdict, D-08 trigger phrases, and full 4-step protocol</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md -> Claude Code skill runtime | Skill file loaded by Claude Code; description field influences trigger matching |
| SKILL.md -> Agent subagents | Orchestrator spawns agents via Task tool; agents receive user context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | SKILL.md `description` trigger phrases | accept | Trigger phrases are used for natural language matching only. No security boundary — user explicitly invokes the skill. D-10 ensures non-overlapping triggers to prevent wrong-council invocation. |
| T-02-04 | Information Disclosure | Advisor context passing | accept | Step 2 passes user-provided context to advisors. No elevation of privilege — advisors have the same tool access the user already has. |
</threat_model>

<verification>
- `skills/council-strategy/SKILL.md` exists and is non-empty
- Frontmatter contains all 3 required fields: name, description, disable-model-invocation
- Step 3 uses D-06 axes (Strategic Question, Viability, Market Fit, Risk/Reward, Competitive Position, Verdict, First Move)
- Step 3 does NOT contain code council axes (agreements, clashes, blind spots)
- Verdict instruction explicitly states categorical (Go/No-Go/Pivot, no hedging)
- Step 2 uses parallel spawn pattern with anti-anchoring instruction
</verification>

<success_criteria>
Strategy SKILL.md orchestrator exists with:
- Custom synthesis structure (D-06) completely distinct from code council
- Three-value categorical verdict (D-07)
- Strategy-specific trigger phrases (D-08) with no overlap to code council (D-10)
- Parallel spawn protocol identical to code council's anti-anchoring design
- `disable-model-invocation: true` preventing auto-load (FOUND-03 pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/02-council-strategy/02-02-SUMMARY.md`
</output>

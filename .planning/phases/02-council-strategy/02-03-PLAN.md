---
phase: 02-council-strategy
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - install.sh
autonomous: false
requirements:
  - STRT-01
  - STRT-04

must_haves:
  truths:
    - "install.sh SKILLS array includes council-strategy"
    - "install.sh PERSONAS array includes all 5 strategy advisor names"
    - "install.sh parses without syntax errors"
    - "User can verify strategy council invocation after running install.sh"
  artifacts:
    - path: "install.sh"
      provides: "Updated installer with strategy council entries"
      contains: "strategy-devils-advocate"
  key_links:
    - from: "install.sh SKILLS array"
      to: "skills/council-strategy/"
      via: "install_link/install_copy loop"
      pattern: "council-strategy"
    - from: "install.sh PERSONAS array"
      to: "agents/strategy-*.md"
      via: "install_link/install_copy loop appends .md"
      pattern: "strategy-devils-advocate"
---

<objective>
Update install.sh to include strategy council in SKILLS and PERSONAS arrays, then verify end-to-end installation works.

Purpose: Wires the new strategy agent files and SKILL.md into the installer so `./install.sh` deploys the complete strategy council alongside the existing code council. Without this update, the strategy files exist in the repo but are not installed to `~/.claude/`.

Output: Updated install.sh with strategy entries in SKILLS and PERSONAS arrays.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-council-strategy/02-CONTEXT.md
@.planning/phases/02-council-strategy/02-RESEARCH.md

<!-- Prior plan SUMMARYs needed — this plan modifies install.sh which Plan 01 and Plan 02 created files for -->
@.planning/phases/02-council-strategy/02-01-SUMMARY.md
@.planning/phases/02-council-strategy/02-02-SUMMARY.md

<interfaces>
<!-- Current install.sh arrays that must be updated -->

From install.sh (line 132-134 — current state after Phase 1):
```bash
SKILLS=( council-code council-update )
PERSONAS=( code-contrarian code-first-principles code-expansionist code-outsider code-executor )
HOOKS=( council-statusline.js council-check-update.js council-check-update-worker.js patch-settings.js )
```

From install.sh (line 162-168 — install loop pattern, no .md suffix in array):
```bash
if [[ "$MODE" == "symlink" ]]; then
  for skill in "${SKILLS[@]}"; do
    install_link "$REPO_ROOT/skills/$skill" "$SKILLS_DIR/$skill"
  done
  for persona in "${PERSONAS[@]}"; do
    install_link "$REPO_ROOT/agents/$persona.md" "$AGENTS_DIR/$persona.md"
  done
```

From install.sh (line 136-157 — uninstall block with LEGACY_PERSONAS):
```bash
if [[ "$MODE" == "uninstall" ]]; then
  # ... LEGACY_PERSONAS cleanup for Phase 1 bare-name migration
  LEGACY_PERSONAS=( contrarian first-principles expansionist outsider executor )
  # ...
```

NOTE per RESEARCH.md anti-patterns: Do NOT add strategy names to LEGACY_PERSONAS. Strategy agents never had bare names — no legacy cleanup needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strategy entries to install.sh SKILLS and PERSONAS arrays</name>
  <files>install.sh</files>
  <read_first>
    - install.sh (current state — verify exact line numbers and array contents before editing)
    - .planning/phases/02-council-strategy/02-RESEARCH.md (install.sh array update pattern, anti-pattern about LEGACY_PERSONAS)
  </read_first>
  <action>
Edit install.sh to update exactly 2 lines:

**Line 132 — SKILLS array**: Change from:
```bash
SKILLS=( council-code council-update )
```
to:
```bash
SKILLS=( council-code council-update council-strategy )
```

**Line 133 — PERSONAS array**: Change from:
```bash
PERSONAS=( code-contrarian code-first-principles code-expansionist code-outsider code-executor )
```
to:
```bash
PERSONAS=( code-contrarian code-first-principles code-expansionist code-outsider code-executor \
           strategy-devils-advocate strategy-visionary strategy-pragmatist strategy-customer-champion strategy-operator )
```

The line continuation backslash + indent is for readability. The array entries do NOT include `.md` suffix — the install loop appends `.md` when calling `install_link`/`install_copy` (verified at line 167: `"$REPO_ROOT/agents/$persona.md"`).

Do NOT modify:
- The HOOKS array (line 134) — no new hooks for strategy council
- The LEGACY_PERSONAS array (line 140) — strategy agents never had bare names
- Any other part of install.sh

After editing, verify the script still parses: `bash -n install.sh`
  </action>
  <verify>
    <automated>cd /Users/D052192/src/council-code && bash -n install.sh && echo "PASS: script parses" && grep "council-strategy" install.sh && grep "strategy-devils-advocate" install.sh && grep "strategy-visionary" install.sh && grep "strategy-pragmatist" install.sh && grep "strategy-customer-champion" install.sh && grep "strategy-operator" install.sh && echo "--- checking LEGACY_PERSONAS not modified ---" && grep "LEGACY_PERSONAS" install.sh | grep -v "strategy"; test $? -eq 0 && echo "PASS: LEGACY_PERSONAS unchanged"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n install.sh` exits 0 (no syntax errors)
    - install.sh SKILLS array contains `council-strategy` (grep "council-strategy" install.sh matches)
    - install.sh PERSONAS array contains `strategy-devils-advocate` (grep matches)
    - install.sh PERSONAS array contains `strategy-visionary` (grep matches)
    - install.sh PERSONAS array contains `strategy-pragmatist` (grep matches)
    - install.sh PERSONAS array contains `strategy-customer-champion` (grep matches)
    - install.sh PERSONAS array contains `strategy-operator` (grep matches)
    - LEGACY_PERSONAS array does NOT contain any "strategy-" entries
    - HOOKS array is unchanged (still has exactly 4 entries: council-statusline.js, council-check-update.js, council-check-update-worker.js, patch-settings.js)
    - `bash install.sh --help` exits 0 and prints usage
  </acceptance_criteria>
  <done>install.sh updated with strategy council entries in SKILLS and PERSONAS arrays, script parses cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify strategy council end-to-end</name>
  <what-built>
All Phase 2 artifacts are now complete:
- 5 strategy advisor agent files in agents/
- Strategy SKILL.md orchestrator in skills/council-strategy/
- install.sh updated with strategy entries

This checkpoint verifies the complete strategy council is functional.
  </what-built>
  <how-to-verify>
1. Run `./install.sh` from the repo root to install strategy agents and skill
2. Verify symlinks were created:
   - `ls -la ~/.claude/agents/strategy-*.md` — should show 5 symlinks
   - `ls -la ~/.claude/skills/council-strategy/` — should show symlinked SKILL.md
3. Restart Claude Code (close and reopen)
4. Test individual advisor invocation (STRT-04):
   - Type: "get the Devil's Advocate view on whether we should raise prices 20%"
   - Expected: The strategy-devils-advocate agent loads and produces output with "Fatal Assumption" and "Bear Case" sections
5. Test full council invocation (STRT-01, STRT-03):
   - Type: `/council-strategy` followed by "Should we expand into the European market?"
   - Expected: 5 advisors spawn in parallel, chairman synthesis uses strategy-specific axes (Viability Assessment, Market Fit, Risk/Reward, Competitive Position), verdict is exactly one of Go/No-Go/Pivot
6. Verify trigger isolation (D-10):
   - Type: "stress test this approach" — should invoke code council, NOT strategy council
   - Type: "evaluate this pricing strategy" — should invoke strategy council, NOT code council
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh -> filesystem | Script creates symlinks in ~/.claude/ with user's permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | install.sh array modification | accept | Array append follows established pattern. Install loop uses existing install_link/install_copy helpers. No new filesystem operations introduced. |
| T-02-06 | Elevation of Privilege | Symlinked agent files | accept | Symlinks point to repo-owned files. User already has read access to the repo. No privilege escalation — Claude Code agents run with user's existing permissions. |
</threat_model>

<verification>
- `bash -n install.sh` exits 0
- `bash install.sh --help` exits 0 and shows usage text
- `grep -c "strategy-" install.sh` returns count >= 6 (1 in SKILLS + 5 in PERSONAS)
- After running `./install.sh`: `ls ~/.claude/agents/strategy-*.md | wc -l` returns 5
- After running `./install.sh`: `ls ~/.claude/skills/council-strategy/SKILL.md` returns the file
</verification>

<success_criteria>
install.sh updated and strategy council installable:
- SKILLS array includes council-strategy
- PERSONAS array includes all 5 strategy advisor names (without .md suffix)
- Script parses and runs without error
- After installation, all 5 strategy agents and the strategy SKILL.md are available in ~/.claude/
- Human verification confirms individual and full council invocation works
</success_criteria>

<output>
After completion, create `.planning/phases/02-council-strategy/02-03-SUMMARY.md`
</output>

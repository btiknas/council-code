---
phase: 02-council-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/strategy-devils-advocate.md
  - agents/strategy-visionary.md
  - agents/strategy-pragmatist.md
  - agents/strategy-customer-champion.md
  - agents/strategy-operator.md
autonomous: true
requirements:
  - STRT-01
  - STRT-02
  - STRT-04

must_haves:
  truths:
    - "5 strategy agent files exist in agents/ with strategy- prefix"
    - "Each advisor has a unique output format with strategy-native section names"
    - "No code/engineering vocabulary appears in advisor mandates or output templates"
    - "Each advisor's description frontmatter enables individual invocation by name"
  artifacts:
    - path: "agents/strategy-devils-advocate.md"
      provides: "Assumption-challenging strategy advisor"
      contains: "Fatal Assumption"
    - path: "agents/strategy-visionary.md"
      provides: "Long-horizon opportunity advisor"
      contains: "10-Year Position"
    - path: "agents/strategy-pragmatist.md"
      provides: "Execution feasibility advisor"
      contains: "Reality Check"
    - path: "agents/strategy-customer-champion.md"
      provides: "Buyer perspective advisor"
      contains: "Willingness to Pay"
    - path: "agents/strategy-operator.md"
      provides: "Execution plan advisor"
      contains: "90-Day Execution Plan"
  key_links:
    - from: "agents/strategy-*.md"
      to: "Claude Code agent runtime"
      via: "description frontmatter field"
      pattern: "description:.*strategy"
---

<objective>
Write all 5 strategy council advisor agent files with domain-native personas, unique output templates, and individually-invokable descriptions.

Purpose: Delivers the advisory roster (STRT-01) with unique output contracts (STRT-02) and individual invocation (STRT-04). These are the core intellectual artifacts of Phase 2 — the strategy-specific personas that make this council distinct from the code council.

Output: 5 new agent files in agents/ directory: strategy-devils-advocate.md, strategy-visionary.md, strategy-pragmatist.md, strategy-customer-champion.md, strategy-operator.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-council-strategy/02-CONTEXT.md
@.planning/phases/02-council-strategy/02-RESEARCH.md
@.planning/phases/02-council-strategy/02-PATTERNS.md

<interfaces>
<!-- Structural template from existing code council agents. Replicate this structure exactly, substituting strategy domain content. -->

From agents/code-contrarian.md (frontmatter):
```yaml
---
name: code-contrarian
description: Devil's advocate for code decisions. Finds the fatal flaw, challenges consensus, surfaces hidden failure modes in proposed architectures, libraries, APIs, refactors, or algorithms.
tools: Read, Grep, Glob, WebSearch, WebFetch
---
```

From agents/code-contrarian.md (body structure):
```markdown
# The Contrarian (Code Focus)

You are the Contrarian on a five-person code advisory council. Your one job: **find the fatal flaw**.

## Mandate
[2-3 sentences]

## How you analyze code questions
1. **[Step].** [Description]
... (4-6 steps)
Read the relevant files provided in context. Reference specific code...

## Output format
```
## [Section 1]
...
## Confidence
[High / Medium / Low / Speculative]
```

## Rules
- [constraint 1]
...
```

From agents/code-executor.md (tools field — note Bash instead of WebSearch/WebFetch):
```yaml
tools: Read, Grep, Glob, Bash
```

NOTE: Strategy advisors operate in a business context, not a codebase. All 5 strategy advisors use `tools: Read, Grep, Glob, WebSearch, WebFetch` (allows market research). This is Claude's discretion per CONTEXT.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Devil's Advocate, Visionary, and Pragmatist agent files</name>
  <files>agents/strategy-devils-advocate.md, agents/strategy-visionary.md, agents/strategy-pragmatist.md</files>
  <read_first>
    - agents/code-contrarian.md (structural template for Devil's Advocate — frontmatter schema, section ordering, output format fencing convention)
    - agents/code-expansionist.md (structural template for Visionary — opportunity-lens mandate pattern)
    - agents/code-first-principles.md (structural template for Pragmatist — constraint-questioning lens pattern)
    - .planning/phases/02-council-strategy/02-RESEARCH.md (persona mandates, output template section names, anti-patterns to avoid)
    - .planning/phases/02-council-strategy/02-PATTERNS.md (exact analog mappings and application instructions per file)
  </read_first>
  <action>
Create 3 new agent files following the exact structural template from code council agents. Each file has 6 sections in this order: YAML frontmatter, H1 title with bold one-liner, ## Mandate, ## How you analyze strategy questions, ## Output format (fenced code block), ## Rules.

**agents/strategy-devils-advocate.md** (per D-01, D-02, D-03, D-04, D-05):
- Frontmatter: `name: strategy-devils-advocate`, `description:` must contain "Devil's Advocate", "strategy", "challenge", "assumption testing", "what could go wrong" — these are the natural language triggers for individual invocation (per D-09), `tools: Read, Grep, Glob, WebSearch, WebFetch`
- H1: `# The Devil's Advocate (Strategy Focus)`
- One-liner job: `**find why this strategy fails**`
- Mandate: Establish investment due diligence / pre-mortem worldview. The advisor assumes the strategy will fail and investigates how. NOT a code reviewer — a business risk assessor. No code vocabulary.
- Analysis steps: 4-6 numbered steps using strategy verbs: challenge assumptions, stress-test the business case, map competitive response, identify the falsifiable hypothesis. End with: "Read the relevant files provided in context. Reference specific data, market claims, or financial projections when your analysis depends on factual details."
- Output format sections (per RESEARCH.md Persona Design, each in its own ## heading inside the fenced block):
  - `## Fatal Assumption` — the single belief the strategy depends on most, that might be wrong
  - `## What Would Have to Be True` — conditions that must hold for this to succeed
  - `## The Bear Case` — what the strategy looks like if 2-3 key assumptions are wrong
  - `## Competitive Response` — how rational competitors react, does that invalidate the plan
  - `## The One Number to Track` — the leading indicator that would falsify the strategy early
  - `## Confidence` — High / Medium / Low
- Rules: 4-6 bullet constraints. Must include: be specific about which assumption fails and why, cite market data or financial projections not vague fears, limit bear case to the 2-3 most likely scenarios, if no fatal assumption exists say so plainly.

**agents/strategy-visionary.md** (per D-01, D-02, D-03, D-04, D-05):
- Frontmatter: `name: strategy-visionary`, `description:` must contain "Visionary", "strategy", "long-term opportunity", "10-year", "market positioning", "what this could become"
- H1: `# The Visionary (Strategy Focus)`
- One-liner job: `**find the 10x opportunity the proposal undersells**`
- Mandate: Establish venture/long-horizon strategy worldview. Zooms out from the immediate decision to the market position it creates in 10 years. NOT a code architect — a market strategist.
- Analysis steps: 4-6 steps using strategy verbs: identify the larger market, extrapolate trends, find adjacencies, compare minimum viable vs full-potential version. End with context-reading instruction.
- Output format sections:
  - `## The Bigger Market` — the opportunity the current framing undersells
  - `## 10-Year Position` — if this works, what does the company look like in 10 years
  - `## Trend Tailwinds` — macro forces that accelerate this in ways the proposal doesn't claim credit for
  - `## The Adjacent Bet` — one adjacency this strategy unlocks that hasn't been named
  - `## What This Could Become` — minimum viable version vs full-potential version
  - `## Confidence` — High / Medium / Low / Speculative
- Rules: 4-6 constraints. Must include: ground every opportunity in observable market signals not wishful thinking, distinguish free upside from investment upside, the 10-year position must be plausible not aspirational, if the proposal already captures the full opportunity say so.

**agents/strategy-pragmatist.md** (per D-01, D-02, D-03, D-04, D-05):
- Frontmatter: `name: strategy-pragmatist`, `description:` must contain "Pragmatist", "strategy", "reality check", "execution feasibility", "what's actually achievable", "resource constraints"
- H1: `# The Pragmatist (Strategy Focus)`
- One-liner job: `**anchor this to what's actually achievable**`
- Mandate: Establish operational planning / resource reality worldview. Examines the gap between what the strategy assumes and what the team can actually execute. NOT a code first-principles thinker — a resource realist.
- Analysis steps: 4-6 steps using strategy verbs: assess resource reality, identify the 90-day deliverable, separate real constraints from perceived ones, find what to cut to test the core bet. End with context-reading instruction.
- Output format sections:
  - `## Reality Check` — the gap between the plan's assumptions and the team's actual capacity
  - `## Resource Requirements` — what this actually costs (time, money, headcount, attention)
  - `## The 90-Day Version` — what can be validated or shipped in 90 days with current resources
  - `## Inherited Constraints` — which constraints are real vs perceived (mark each)
  - `## What to Cut` — the minimum viable version of this strategy that still tests the core bet
  - `## Confidence` — High / Medium / Low
- Rules: 4-6 constraints. Must include: be specific about resource numbers not vague "it's expensive", distinguish real constraints from organizational habits, the 90-day version must be a real test of the core bet not a watered-down version, if the plan is already realistic say so.

CRITICAL per D-05: After writing each file, verify no code/engineering terms appear in mandate or output template sections. Words to avoid: bug, refactor, technical debt, runtime, edge case, deployment, codebase, PR, commit, merge, lint, test suite. Strategy-native vocabulary only: viability, market fit, competitive position, ROI, willingness to pay, execution capacity, resource allocation.

CRITICAL per D-10: No trigger phrases that overlap with code council. Do not use: "stress test", "second opinion on code", "code review", "engineering decision". Use only: strategy-specific phrases like "challenge this plan", "long-term opportunity", "reality check on this strategy".
  </action>
  <verify>
    <automated>cd /Users/D052192/src/council-code && ls agents/strategy-devils-advocate.md agents/strategy-visionary.md agents/strategy-pragmatist.md && grep -l "name: strategy-" agents/strategy-devils-advocate.md agents/strategy-visionary.md agents/strategy-pragmatist.md && grep -c "Fatal Assumption" agents/strategy-devils-advocate.md && grep -c "10-Year Position" agents/strategy-visionary.md && grep -c "Reality Check" agents/strategy-pragmatist.md && echo "--- checking no code vocabulary in mandates ---" && grep -iE "bug|refactor|technical debt|runtime|edge case|codebase|lint|test suite" agents/strategy-devils-advocate.md agents/strategy-visionary.md agents/strategy-pragmatist.md; test $? -eq 1 && echo "PASS: no code vocabulary found"</automated>
  </verify>
  <acceptance_criteria>
    - agents/strategy-devils-advocate.md exists and contains `name: strategy-devils-advocate` in frontmatter
    - agents/strategy-devils-advocate.md contains `description:` with "Devil's Advocate" and "strategy"
    - agents/strategy-devils-advocate.md contains `tools: Read, Grep, Glob, WebSearch, WebFetch`
    - agents/strategy-devils-advocate.md contains `## Fatal Assumption` and `## The Bear Case` and `## Confidence`
    - agents/strategy-devils-advocate.md contains `## Mandate` and `## How you analyze strategy questions` and `## Output format` and `## Rules`
    - agents/strategy-devils-advocate.md does NOT contain `disable-model-invocation`
    - agents/strategy-visionary.md exists and contains `name: strategy-visionary`
    - agents/strategy-visionary.md contains `## The Bigger Market` and `## 10-Year Position` and `## Trend Tailwinds`
    - agents/strategy-pragmatist.md exists and contains `name: strategy-pragmatist`
    - agents/strategy-pragmatist.md contains `## Reality Check` and `## The 90-Day Version` and `## What to Cut`
    - None of the 3 files contain code-domain words (bug, refactor, technical debt, runtime, edge case, codebase, lint, test suite) in mandate or output sections
  </acceptance_criteria>
  <done>3 strategy agent files created with unique output templates, strategy-native vocabulary, and individually-invokable description frontmatter</done>
</task>

<task type="auto">
  <name>Task 2: Write Customer Champion and Operator agent files</name>
  <files>agents/strategy-customer-champion.md, agents/strategy-operator.md</files>
  <read_first>
    - agents/code-outsider.md (structural template for Customer Champion — external-perspective lens pattern)
    - agents/code-executor.md (structural template for Operator — action/execution lens pattern)
    - agents/strategy-devils-advocate.md (just created — verify consistency of frontmatter schema and section ordering)
    - .planning/phases/02-council-strategy/02-RESEARCH.md (persona mandates, output template section names)
    - .planning/phases/02-council-strategy/02-PATTERNS.md (analog mappings for customer-champion and operator)
  </read_first>
  <action>
Create 2 new agent files following the same structural template used in Task 1. Verify consistency with the 3 files already created (same frontmatter schema, same section order, same fencing convention).

**agents/strategy-customer-champion.md** (per D-01, D-02, D-03, D-04, D-05):
- Frontmatter: `name: strategy-customer-champion`, `description:` must contain "Customer Champion", "strategy", "buyer perspective", "willingness to pay", "what customers will actually do" — natural language triggers for individual invocation (per D-09), `tools: Read, Grep, Glob, WebSearch, WebFetch`
- H1: `# The Customer Champion (Strategy Focus)`
- One-liner job: `**represent the buyer's actual decision process**`
- Mandate: Establish customer development / pricing research worldview. Represents the voice of the buyer — not what the company hopes customers will do, but what they will actually do. Surfaces the gap between business assumptions about customer behavior and observable reality. NOT a code outsider — a buyer behavior analyst.
- Analysis steps: 4-6 numbered steps using customer/market verbs: assess willingness to pay, identify the behavior gap between company expectations and customer reality, profile the actual buyer vs intended buyer, surface objections customers won't say out loud, find the strongest market signal for demand. End with: "Read the relevant files provided in context. Reference specific market data, customer quotes, pricing benchmarks, or competitive offerings when your analysis depends on buyer behavior."
- Output format sections (each in its own ## heading inside fenced code block):
  - `## Willingness to Pay` — what customers will actually pay vs what the strategy assumes
  - `## What Customers Will Actually Do vs. What You Expect` — the behavior gap
  - `## Who This Is Really For` — the actual buyer profile vs the intended profile
  - `## The Objection They Won't Say Out Loud` — the real reason customers hesitate
  - `## Market Signal` — the strongest evidence for customer desire (not projections)
  - `## Confidence` — High / Medium / Low
- Rules: 4-6 bullet constraints. Must include: cite observable customer behavior not company projections, the willingness-to-pay assessment must reference comparable products or market benchmarks, distinguish what customers say they want from what they actually buy, if the strategy correctly reads the customer say so plainly.

**agents/strategy-operator.md** (per D-01, D-02, D-03, D-04, D-05):
- Frontmatter: `name: strategy-operator`, `description:` must contain "Operator", "strategy", "execution plan", "first 90 days", "concrete next steps", "how to actually do this" — natural language triggers for individual invocation (per D-09), `tools: Read, Grep, Glob, WebSearch, WebFetch`
- H1: `# The Operator (Strategy Focus)`
- One-liner job: `**turn this decision into a concrete execution plan**`
- Mandate: Establish COO / general management execution worldview. The other four advisors analyze. The Operator ships. Output must answer "what does the team do in the first 90 days?" with enough specificity that execution could start without re-reading the analysis. NOT a code executor — a business operations planner.
- Analysis steps: 4-6 numbered steps using execution/operations verbs: pick a direction and commit, define the first concrete action with owner and deadline, build a 90-day milestone plan, identify the critical path blocker, name the risks being accepted to keep moving. End with: "Read the relevant files provided in context. Reference specific people, teams, budgets, or timelines when your plan depends on organizational details."
- Output format sections:
  - `## First Action` — the literal first step, with owner and deadline
  - `## 90-Day Execution Plan` — 3-5 milestones, each observable and datable
  - `## Resource Requirements` — specific: headcount, budget, tools, external dependencies
  - `## Critical Path` — the one thing that blocks everything else
  - `## Risk I'm Accepting` — known risks being taken to keep moving; explicit, not hidden
  - `## Confidence` — High / Medium / Low
- Rules: 4-6 bullet constraints. Must include: every milestone must have an observable outcome and a realistic date, no "we should consider" — commit to a direction or explicitly defer, the first action must be specific enough to start without re-reading, if the strategy is too vague to plan say so and demand the specific input needed.

CRITICAL: Verify consistency across all 5 agent files:
1. All use `tools: Read, Grep, Glob, WebSearch, WebFetch` (strategy advisors need web research, not Bash)
2. All follow the same 6-section order: frontmatter, H1+one-liner, Mandate, How you analyze, Output format, Rules
3. All output formats end with `## Confidence`
4. No `disable-model-invocation` in any agent file
5. No code/engineering vocabulary in mandates or output templates (per D-05)
  </action>
  <verify>
    <automated>cd /Users/D052192/src/council-code && ls agents/strategy-customer-champion.md agents/strategy-operator.md && grep -l "name: strategy-" agents/strategy-customer-champion.md agents/strategy-operator.md && grep -c "Willingness to Pay" agents/strategy-customer-champion.md && grep -c "90-Day Execution Plan" agents/strategy-operator.md && echo "--- checking all 5 files exist ---" && ls agents/strategy-*.md | wc -l | tr -d ' ' && echo "--- checking no code vocabulary ---" && grep -iE "bug|refactor|technical debt|runtime|edge case|codebase|lint|test suite" agents/strategy-customer-champion.md agents/strategy-operator.md; test $? -eq 1 && echo "PASS: no code vocabulary found" && echo "--- checking tools field consistency ---" && grep "tools:" agents/strategy-*.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/strategy-customer-champion.md exists and contains `name: strategy-customer-champion` in frontmatter
    - agents/strategy-customer-champion.md contains `description:` with "Customer Champion" and "buyer perspective"
    - agents/strategy-customer-champion.md contains `tools: Read, Grep, Glob, WebSearch, WebFetch`
    - agents/strategy-customer-champion.md contains `## Willingness to Pay` and `## Who This Is Really For` and `## Market Signal`
    - agents/strategy-operator.md exists and contains `name: strategy-operator`
    - agents/strategy-operator.md contains `## First Action` and `## 90-Day Execution Plan` and `## Critical Path`
    - agents/strategy-operator.md contains `tools: Read, Grep, Glob, WebSearch, WebFetch` (NOT `Bash`)
    - `ls agents/strategy-*.md | wc -l` outputs 5
    - All 5 agent files contain `## Mandate`, `## How you analyze strategy questions`, `## Output format`, `## Rules`
    - No agent file contains `disable-model-invocation`
    - None of the 5 files contain code-domain words in mandate or output sections
  </acceptance_criteria>
  <done>All 5 strategy advisor agent files exist with unique output templates, consistent structure, strategy-native vocabulary throughout, and individually-invokable description frontmatter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent file -> Claude Code runtime | Agent files are loaded by Claude Code; description field influences invocation matching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Agent `description` field | accept | Description is used for natural language matching only; no authentication boundary. If a user installs a malicious agent file, they already have filesystem access. Low risk. |
| T-02-02 | Information Disclosure | Agent file content | accept | Agent files contain prompt instructions only, no secrets or PII. All content is intended to be user-visible. |
</threat_model>

<verification>
- `ls agents/strategy-*.md | wc -l` outputs exactly 5
- Each file contains valid YAML frontmatter with `name`, `description`, `tools` fields
- Each file has unique output format sections (no two agents share the same section names except `## Confidence`)
- `grep -rL "## Mandate" agents/strategy-*.md` outputs nothing (all have Mandate)
- `grep -rL "## Output format" agents/strategy-*.md` outputs nothing (all have Output format)
- `grep -rL "## Rules" agents/strategy-*.md` outputs nothing (all have Rules)
- No code vocabulary in strategy agent content (grep for bug, refactor, technical debt, runtime, edge case, codebase, lint, test suite returns no matches)
</verification>

<success_criteria>
5 strategy advisor agent files exist in agents/ directory, each with:
- Unique strategy-native persona (D-01, D-02)
- Correct filename (D-03)
- Unique output format contract with strategy vocabulary (D-04, D-05)
- Description frontmatter enabling individual invocation by name (D-09)
- Non-overlapping trigger phrases vs code council (D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/02-council-strategy/02-01-SUMMARY.md`
</output>

---
phase: 03-router
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/council-code/SKILL.md
  - skills/council-router/SKILL.md
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03

must_haves:
  truths:
    - "council-code description frontmatter contains 'Trigger phrases:' with domain-specific terms and /council-code but NOT /council"
    - "council-code body trigger line says /council-code instead of /council"
    - "council-router SKILL.md exists with frontmatter containing name, description, disable-model-invocation, and allowed-tools"
    - "council-router SKILL.md contains a static list of installed councils (council-code, council-strategy) with descriptions and trigger phrases"
    - "council-router SKILL.md contains a classification protocol that uses inline LLM reasoning (no subagent spawn)"
    - "council-router SKILL.md contains a compact recommendation block format followed by a two-option confirm flow"
    - "council-router SKILL.md dispatches via Skill(skill=...) after user confirmation"
    - "council-router SKILL.md never auto-dispatches, never reformulates the question, never references uninstalled councils"
  artifacts:
    - path: "skills/council-router/SKILL.md"
      provides: "Router orchestrator with classification, suggestion, and dispatch protocol"
      min_lines: 80
    - path: "skills/council-code/SKILL.md"
      provides: "Updated code council with trigger phrases in description frontmatter"
      contains: "Trigger phrases:"
  key_links:
    - from: "skills/council-router/SKILL.md"
      to: "skills/council-code/SKILL.md"
      via: "static council list references council-code description and trigger phrases"
      pattern: "council-code.*Trigger phrases"
    - from: "skills/council-router/SKILL.md"
      to: "skills/council-strategy/SKILL.md"
      via: "static council list references council-strategy description and trigger phrases"
      pattern: "council-strategy.*Trigger phrases"
---

<objective>
Create the council-router skill and update council-code's description frontmatter with trigger phrases.

Purpose: Delivers the core router orchestrator (ROUT-01, ROUT-02) and ensures all council SKILL.md files carry domain-specific trigger phrases in their description frontmatter for natural language detection (ROUT-03). council-strategy already has trigger phrases in its description; council-code does not and needs the update.

Output: `skills/council-router/SKILL.md` (new), `skills/council-code/SKILL.md` (modified description frontmatter + body trigger line)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-router/03-CONTEXT.md
@.planning/phases/03-router/03-RESEARCH.md
@.planning/phases/03-router/03-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From skills/council-code/SKILL.md (current frontmatter, lines 1-5):
```yaml
---
name: council-code
description: Multi-perspective code decision council. Runs 5 expert advisors (Contrarian, First Principles, Expansionist, Outsider, Executor) in parallel on a code/architecture/engineering question, then synthesizes a chairman verdict with agreements, clashes, blind spots, and a concrete next step.
disable-model-invocation: true
---
```

From skills/council-code/SKILL.md (current body trigger line, line 23):
```markdown
Trigger phrases: `/council`, `council`, "get a second opinion," "stress test this approach," "what am I missing," "have the council review."
```

From skills/council-strategy/SKILL.md (frontmatter with trigger phrases -- the target pattern, lines 1-5):
```yaml
---
name: council-strategy
description: Multi-perspective strategy decision council. Runs 5 advisors (Devil's Advocate, Visionary, Pragmatist, Customer Champion, Operator) in parallel on a business or product decision, then synthesizes a chairman verdict with viability assessment, market fit analysis, risk/reward matrix, competitive position, and a Go/No-Go/Pivot call. Trigger phrases: strategy council, evaluate this business decision, should we pivot, pricing strategy, market analysis, business strategy.
disable-model-invocation: true
---
```

From skills/council-update/SKILL.md (allowed-tools + AskUserQuestion pattern, lines 1-7):
```yaml
---
name: council-update
description: Check for council-code updates on GitHub, show the changelog of new commits, and fast-forward the local repo. Mirrors the /gsd-update flow for the council-code skill.
allowed-tools:
  - Bash
  - AskUserQuestion
---
```

From ~/.claude/skills/gsd-manager/SKILL.md (Skill tool in allowed-tools, lines 1-13):
```yaml
---
name: gsd-manager
description: "Interactive command center for managing multiple phases from one terminal"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - AskUserQuestion
  - Skill
  - Task
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add trigger phrases to council-code description frontmatter and update body trigger line</name>
  <files>skills/council-code/SKILL.md</files>

  <read_first>
    skills/council-code/SKILL.md
    skills/council-strategy/SKILL.md
  </read_first>

  <action>
Two changes to `skills/council-code/SKILL.md`:

**Change 1 — Description frontmatter (line 3):**

Replace the current `description` value:
```yaml
description: Multi-perspective code decision council. Runs 5 expert advisors (Contrarian, First Principles, Expansionist, Outsider, Executor) in parallel on a code/architecture/engineering question, then synthesizes a chairman verdict with agreements, clashes, blind spots, and a concrete next step.
```

With this value (appends "Trigger phrases:" suffix matching the council-strategy pattern per ROUT-03):
```yaml
description: Multi-perspective code decision council. Runs 5 expert advisors (Contrarian, First Principles, Expansionist, Outsider, Executor) in parallel on a code/architecture/engineering question, then synthesizes a chairman verdict with agreements, clashes, blind spots, and a concrete next step. Trigger phrases: /council-code, code council, architecture choices, library selection, refactor vs rewrite, performance strategy, API design, code review, debugging hypotheses, stress test this approach, what am I missing.
```

CRITICAL: `/council` is intentionally ABSENT from this list per D-11 — that trigger moves to council-router. `/council-code` is the direct shortcut.

**Change 2 — Body trigger line (line 23):**

Replace:
```markdown
Trigger phrases: `/council`, `council`, "get a second opinion," "stress test this approach," "what am I missing," "have the council review."
```

With:
```markdown
Trigger phrases: `/council-code`, `council-code`, "get a second opinion," "stress test this approach," "what am I missing," "have the council review."
```

This changes `/council` to `/council-code` and `council` to `council-code` in the body. The body trigger phrases are behavioral guidance for the running skill, not routing metadata, but they should be consistent with the frontmatter after `/council` moves to the router (belt-and-suspenders per RESEARCH.md Pitfall 1).

No other lines are changed. The rest of the file remains identical.
  </action>

  <verify>
    <automated>cd /Users/D052192/src/council-code && grep -c "Trigger phrases:" skills/council-code/SKILL.md | grep -q "2" && grep "^description:" skills/council-code/SKILL.md | grep -q "/council-code" && grep "^description:" skills/council-code/SKILL.md | grep -qv "Trigger phrases:.*[^-]\/council[^-]" && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - skills/council-code/SKILL.md frontmatter `description` field contains the string "Trigger phrases: /council-code, code council, architecture choices"
    - skills/council-code/SKILL.md frontmatter `description` field does NOT contain the bare string "/council," (with comma, indicating the old /council trigger without -code suffix)
    - skills/council-code/SKILL.md body (line ~23) contains `/council-code` instead of `/council`
    - skills/council-code/SKILL.md body (line ~23) contains `council-code` instead of bare `council`
    - grep "Trigger phrases:" skills/council-code/SKILL.md returns exactly 2 lines (one in frontmatter description, one in body)
    - skills/council-strategy/SKILL.md is NOT modified (already has trigger phrases)
  </acceptance_criteria>

  <done>council-code SKILL.md has trigger phrases in description frontmatter (matching council-strategy pattern) with /council-code as the direct shortcut, and body trigger line updated to /council-code. /council does not appear as a trigger in either location.</done>
</task>

<task type="auto">
  <name>Task 2: Create council-router SKILL.md orchestrator</name>
  <files>skills/council-router/SKILL.md</files>

  <read_first>
    skills/council-code/SKILL.md
    skills/council-strategy/SKILL.md
    skills/council-update/SKILL.md
    .planning/phases/03-router/03-CONTEXT.md
    .planning/phases/03-router/03-RESEARCH.md
    .planning/phases/03-router/03-PATTERNS.md
  </read_first>

  <action>
Create the directory `skills/council-router/` and the file `skills/council-router/SKILL.md`.

**Frontmatter** (compose from council-code base + council-update allowed-tools + gsd-manager Skill tool):

```yaml
---
name: council-router
description: Smart council selector. Analyzes your question and suggests the most appropriate council (code, strategy) with reasoning, then dispatches after your confirmation. Trigger phrases: which council, route this, help me pick a council, /council, /council-router.
disable-model-invocation: true
allowed-tools:
  - AskUserQuestion
  - Skill
---
```

CRITICAL frontmatter notes:
- `description` MUST include `/council` as a trigger phrase per D-11 — this is how `/council` gets reassigned from council-code to council-router.
- `disable-model-invocation: true` per FOUND-03 convention.
- `allowed-tools` MUST list both `AskUserQuestion` (for confirm flow) and `Skill` (for dispatch). This is required because these tools are not available by default in skill execution — they need explicit declaration (per Pitfall 5 in RESEARCH.md, confirmed by gsd-manager pattern).

**Body structure** (follow council SKILL.md protocol pattern from 03-PATTERNS.md):

The body must contain these sections in this order:

**1. Title and tagline:**
```markdown
# council-router

Smart entry point for the council system. Analyzes your question, suggests the best-fit council, and dispatches after you confirm.
```

**2. "When to use this skill" section** (per D-13 — explicit router triggers ONLY):
```markdown
## When to use this skill

Invoke when the user doesn't know which council to use and wants a recommendation:

- "Which council should I use for this?"
- "Route this question"
- "Help me pick a council"
- `/council` (smart entry point — suggests the right council)
- `/council-router` (direct invocation)

## When NOT to use

- User already knows which council they want — they should use `/council-code` or `/council-strategy` directly.
- Ambiguous triggers like "second opinion," "stress test this," "what am I missing" — these belong to `/council-code`, not the router.
- Simple questions that don't need a council at all — just answer directly.
```

**3. "Installed Councils" section** (per D-02 — static list, updated manually when new councils ship):
```markdown
## Installed Councils

**council-code** — Multi-perspective code decision council. Runs 5 expert advisors (Contrarian, First Principles, Expansionist, Outsider, Executor) in parallel on a code/architecture/engineering question, then synthesizes a chairman verdict with agreements, clashes, blind spots, and a concrete next step.
Trigger phrases: architecture choices, library selection, refactor vs rewrite, performance strategy, API design, code review, debugging hypotheses, stress test this approach, what am I missing.

**council-strategy** — Multi-perspective strategy decision council. Runs 5 advisors (Devil's Advocate, Visionary, Pragmatist, Customer Champion, Operator) in parallel on a business or product decision, then synthesizes a chairman verdict with viability assessment, market fit analysis, risk/reward matrix, competitive position, and a Go/No-Go/Pivot call.
Trigger phrases: pricing strategy, market entry, product roadmap, partnership evaluation, competitive positioning, go-to-market, resource allocation, evaluate this business decision, should we pivot.
```

Note: Only `council-code` and `council-strategy` per D-09 — do NOT reference design, research, or review councils.

**4. "Protocol" section** with 3 steps (classify, suggest, dispatch):

```markdown
## Protocol

### Step 1 — Classify the question

Read the user's question. Compare it against the council descriptions and trigger phrases listed in "Installed Councils" above. Select the council whose domain coverage most closely matches the decision the user is making.

**Classification rules:**
- If the question is about code, architecture, engineering, libraries, APIs, refactoring, performance, or debugging — suggest **council-code**.
- If the question is about business strategy, pricing, market entry, product roadmap, partnerships, competitive positioning, or go-to-market — suggest **council-strategy**.
- If the question spans multiple domains, pick the council that covers the PRIMARY decision type. Note the ambiguity in the recommendation.
- If no council is a clear fit, pick the closest match and flag low confidence. Never refuse to suggest.

Do NOT spawn a subagent for classification. This is a single inference step — read the question, reason about fit, select a council.

### Step 2 — Present the recommendation

Present a compact recommendation block:

**Standard confidence:**
```
**Suggested council: [council-name]**

[1-2 sentence reasoning: what the question is about and why this council fits]

-> Run [council-name], or pick a different council?
  A) Run [council-name]
  B) Pick a different council
```

**Low confidence (ambiguous fit):**
```
**Suggested council: [council-name]** _(low confidence — this could also fit [other-council])_

[1-2 sentence reasoning explaining the ambiguity]

-> Run [council-name], or pick a different council?
  A) Run [council-name]
  B) Pick a different council
```

Use `AskUserQuestion` to present the A/B options and wait for the user's response. Do NOT proceed to Step 3 until the user has confirmed.

CRITICAL: Never auto-dispatch. Always present the recommendation and wait. The cost of running the wrong council (5 parallel agent runs) is too high to skip confirmation.

### Step 3 — Dispatch

**If user selects A (Run suggested council):**

Invoke the suggested council using the Skill tool:

```
Skill(skill="council-code")
```
or
```
Skill(skill="council-strategy")
```

Do NOT pass `args`. The user's original question is already in the conversation context. Per D-07: do not reformulate, restate, or add context — the council's own SKILL.md handles context extraction from the conversation.

**If user selects B (Pick a different council):**

Show the full list of installed councils:
```
Available councils:
1. council-code — Code, architecture, and engineering decisions
2. council-strategy — Business, product, and strategy decisions

Which council would you like to run?
```

Use `AskUserQuestion` to let the user pick. Then dispatch via `Skill(skill="...")` as above.
```

**5. "Guardrails" section:**
```markdown
## Guardrails

- **Never auto-dispatch without confirmation.** Always present the recommendation and wait for the user to confirm. Even for obvious matches — the confirm step is mandatory (ROUT-02).
- **Never reformulate the question.** Pass the user's original question as-is to the council. The council's SKILL.md handles context extraction (D-07).
- **Never reference uninstalled councils.** Only suggest councils in the "Installed Councils" list. Do not mention future councils (design, research, review) that have not shipped yet (D-09).
- **Never use a subagent for classification.** Classification is one inference step — inline reasoning, not a Task spawn. No subagent overhead for a routing decision (D-01).
- **Never refuse to suggest.** If no council is a clear fit, pick the closest match and flag low confidence. The user can always override (D-04).
- **Do not intercept ambiguous triggers.** "Second opinion," "stress test this," "what am I missing" — these belong to council-code. The router only activates on explicit routing requests (D-13).
```

The executor should write the complete file following this structure. The exact wording of the classification reasoning, recommendation block formatting details (bold, spacing), and the "Pick a different council" list presentation are Claude's discretion per CONTEXT.md. The structure, protocol flow, and guardrails are non-negotiable.
  </action>

  <verify>
    <automated>cd /Users/D052192/src/council-code && test -f skills/council-router/SKILL.md && grep -q "name: council-router" skills/council-router/SKILL.md && grep -q "disable-model-invocation: true" skills/council-router/SKILL.md && grep -q "Skill" skills/council-router/SKILL.md && grep -q "AskUserQuestion" skills/council-router/SKILL.md && grep -q "council-code" skills/council-router/SKILL.md && grep -q "council-strategy" skills/council-router/SKILL.md && grep -q "Never auto-dispatch" skills/council-router/SKILL.md && grep -q "/council" skills/council-router/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - File `skills/council-router/SKILL.md` exists
    - Frontmatter contains `name: council-router`
    - Frontmatter contains `description:` with "Smart council selector" and "Trigger phrases:" including "/council" and "/council-router"
    - Frontmatter contains `disable-model-invocation: true`
    - Frontmatter contains `allowed-tools:` listing both `AskUserQuestion` and `Skill`
    - Body contains `## When to use this skill` section with explicit routing triggers only
    - Body contains `## When NOT to use` section mentioning "second opinion" and "stress test" as NOT router triggers
    - Body contains `## Installed Councils` section listing exactly council-code and council-strategy with descriptions and trigger phrases
    - Body does NOT contain "council-design" or "council-research" or "council-review" (uninstalled councils per D-09)
    - Body contains `## Protocol` with `### Step 1`, `### Step 2`, `### Step 3`
    - Step 1 contains classification logic with inline LLM reasoning (no Task/subagent spawn)
    - Step 2 contains compact recommendation block format with A/B options
    - Step 2 contains "AskUserQuestion" reference for confirmation
    - Step 2 contains "Never auto-dispatch" or equivalent gating language
    - Step 3 contains `Skill(skill="council-code")` and `Skill(skill="council-strategy")` dispatch examples
    - Step 3 does NOT pass `args` to the Skill invocation (per D-07)
    - Body contains `## Guardrails` section with at least 5 guardrail bullets
    - File has at least 80 lines (substantial orchestrator, not a stub)
  </acceptance_criteria>

  <done>skills/council-router/SKILL.md exists as a complete router orchestrator with frontmatter (name, description with /council trigger, disable-model-invocation, allowed-tools), static council list (council-code and council-strategy only), 3-step classify-suggest-dispatch protocol with AskUserQuestion confirmation gate, and guardrails preventing auto-dispatch, question reformulation, and uninstalled council references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input to router | Natural language question classified by LLM — no code execution, no data persistence |
| Router to council | Skill tool invocation passes conversation context — same trust boundary as direct council invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Router classification | accept | Classification is LLM reasoning over natural language descriptions. No auth, no secrets, no PII involved. Misclassification results in wrong council suggestion, mitigated by mandatory user confirmation (ROUT-02). Low-value target. |
| T-03-02 | Tampering | SKILL.md static council list | accept | Static list is version-controlled in git. Tampering requires repo write access, which already grants full code access. No additional attack surface beyond existing skill files. |
| T-03-03 | Information Disclosure | Conversation context passed to council | accept | Router passes conversation context as-is to the dispatched council via Skill tool. This is identical to direct council invocation — no new information exposure. |
| T-03-04 | Denial of Service | Misrouting burns 5 agent runs | mitigate | Mandatory user confirmation before dispatch (ROUT-02, D-06). User sees the suggestion and can override before any agent runs are consumed. |
</threat_model>

<verification>
1. `grep "Trigger phrases:" skills/council-code/SKILL.md` returns 2 lines — one in frontmatter, one in body
2. `grep "/council-code" skills/council-code/SKILL.md` returns at least 2 matches (frontmatter + body)
3. `grep -c "/council[^-]" skills/council-code/SKILL.md` returns 0 (no bare /council without -code or -router suffix)
4. `skills/council-router/SKILL.md` exists and is at least 80 lines
5. `grep "council-design\|council-research\|council-review" skills/council-router/SKILL.md` returns 0 matches
6. `grep "allowed-tools" skills/council-router/SKILL.md` returns a match
</verification>

<success_criteria>
- council-code description frontmatter has trigger phrases (ROUT-03 gap closed)
- council-router SKILL.md is a complete orchestrator with classification, confirmation gate, and dispatch
- /council trigger phrase appears in council-router description (D-11 reassignment)
- /council does NOT appear in council-code triggers (D-11)
- Router only references installed councils (D-09)
- Router enforces suggest-and-confirm (ROUT-02)
</success_criteria>

<output>
After completion, create `.planning/phases/03-router/03-01-SUMMARY.md`
</output>

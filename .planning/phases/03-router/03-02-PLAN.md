---
phase: 03-router
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - install.sh
autonomous: false
requirements:
  - ROUT-01
  - ROUT-02

must_haves:
  truths:
    - "install.sh SKILLS array contains council-router"
    - "Running ./install.sh creates a symlink at ~/.claude/skills/council-router pointing to the repo skills/council-router directory"
    - "User can invoke /council-router and receive a council suggestion"
    - "User can invoke /council and it routes to council-router (not council-code)"
  artifacts:
    - path: "install.sh"
      provides: "Updated installer with council-router in SKILLS array"
      contains: "council-router"
  key_links:
    - from: "install.sh"
      to: "skills/council-router/SKILL.md"
      via: "SKILLS array entry causes symlink creation during install"
      pattern: "council-router"
---

<objective>
Register council-router in the installer and verify the full routing flow end-to-end.

Purpose: Without the install.sh update, the router skill directory won't be symlinked into ~/.claude/skills/ and /council-router invocation will fail with "skill not found." The checkpoint verifies the entire Phase 3 delivery: /council routes to the router, router suggests a council, user confirms, council dispatches.

Output: Updated `install.sh`, human-verified end-to-end routing flow
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-router/03-01-SUMMARY.md

<interfaces>
<!-- Key patterns from install.sh that the executor needs. -->

From install.sh (current SKILLS array, line 132):
```bash
SKILLS=( council-code council-update council-strategy )
```

From install.sh (install loop, lines 163-168):
```bash
if [[ "$MODE" == "symlink" ]]; then
  for skill in "${SKILLS[@]}"; do
    install_link "$REPO_ROOT/skills/$skill" "$SKILLS_DIR/$skill"
  done
```

From install.sh (uninstall loop, lines 146-148):
```bash
for skill in "${SKILLS[@]}"; do
  remove_target "$SKILLS_DIR/$skill"
done
```

Note: PERSONAS array is NOT modified — council-router has no agent files (pure orchestrator).
HOOKS array is NOT modified — council-router has no hooks.
LEGACY_PERSONAS array is NOT modified — router never had bare-name agents.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add council-router to install.sh SKILLS array</name>
  <files>install.sh</files>

  <read_first>
    install.sh
  </read_first>

  <action>
One change to `install.sh`:

**Line 132 — SKILLS array:**

Replace:
```bash
SKILLS=( council-code council-update council-strategy )
```

With:
```bash
SKILLS=( council-code council-update council-strategy council-router )
```

This is the identical pattern used in Phase 2 when `council-strategy` was added (see 02-03-SUMMARY.md).

No other changes to install.sh:
- PERSONAS array: unchanged (router has no agent files)
- HOOKS array: unchanged (router has no hooks)
- LEGACY_PERSONAS array: unchanged (router never had bare-name agents)
- All install/uninstall loops already iterate over the SKILLS array, so adding the entry handles both install and uninstall automatically.

Also update the final success message on line 188. Replace:
```bash
ok "done. Restart Claude Code, then try: /council-code"
```

With:
```bash
ok "done. Restart Claude Code, then try: /council or /council-code"
```

This reflects that `/council` is now the smart entry point (router) per D-11.
  </action>

  <verify>
    <automated>cd /Users/D052192/src/council-code && bash -n install.sh && grep "SKILLS=" install.sh | grep -q "council-router" && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n install.sh` exits 0 (script parses cleanly)
    - `grep "SKILLS=" install.sh` output contains "council-router"
    - SKILLS array has exactly 4 entries: council-code, council-update, council-strategy, council-router
    - PERSONAS array is unchanged (still has 10 entries, no router personas)
    - HOOKS array is unchanged (still has 4 entries)
    - LEGACY_PERSONAS array is unchanged
    - `bash install.sh --help` exits 0
  </acceptance_criteria>

  <done>install.sh SKILLS array includes council-router. Script parses cleanly. Install and uninstall loops will handle the new skill directory automatically.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end router installation and invocation</name>

  <what-built>
Phase 3 delivers a smart council router:
1. `skills/council-router/SKILL.md` — new router orchestrator (Plan 01)
2. `skills/council-code/SKILL.md` — updated with trigger phrases in description frontmatter (Plan 01)
3. `install.sh` — updated with council-router in SKILLS array (Plan 02, Task 1)
  </what-built>

  <how-to-verify>
**Step 1: Re-install to pick up the new skill**
```bash
cd /Users/D052192/src/council-code
./install.sh
```
Verify output shows "linked ~/.claude/skills/council-router" (or similar).

**Step 2: Restart Claude Code** to load the new skill definitions.

**Step 3: Test /council-router direct invocation**
In Claude Code, type:
```
/council-router Should I use a message queue or direct API calls for my microservice communication?
```
Expected: Router presents a recommendation for `council-code` with reasoning, then A/B options. It does NOT auto-launch a council.

**Step 4: Test /council reassignment (D-11)**
In Claude Code, type:
```
/council What pricing model should I use for my SaaS?
```
Expected: `/council` invokes the router (not council-code directly). Router suggests `council-strategy` with reasoning.

**Step 5: Confirm dispatch**
Select option A to run the suggested council. Expected: The council launches in the same conversation.

**Step 6: Test direct council shortcuts still work**
```
/council-code Should I refactor this module?
```
Expected: council-code launches directly (not routed through the router).
  </how-to-verify>

  <resume-signal>Type "approved" if all 6 steps pass, or describe which step(s) failed and what happened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh to filesystem | Script creates symlinks in ~/.claude/ — same trust boundary as existing install flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | install.sh SKILLS array | accept | install.sh is version-controlled. Tampering requires repo write access. No change in attack surface from adding one more entry to an existing array. |
| T-03-06 | Elevation of Privilege | Symlink creation | accept | install.sh creates symlinks in user-owned ~/.claude/ directory. No elevated privileges required or granted. Same mechanism used for all existing skills. |
</threat_model>

<verification>
1. `bash -n install.sh` exits 0
2. `grep "SKILLS=" install.sh` contains "council-router"
3. After running `./install.sh`: `ls -la ~/.claude/skills/council-router` shows a valid symlink
4. Human verification of routing flow (checkpoint)
</verification>

<success_criteria>
- install.sh installs council-router alongside existing skills
- /council-router invocation works and presents a council suggestion
- /council invocation routes to council-router (not council-code)
- Direct council shortcuts (/council-code, /council-strategy) still work
- Router waits for confirmation before dispatching (never auto-dispatches)
</success_criteria>

<output>
After completion, create `.planning/phases/03-router/03-02-SUMMARY.md`
</output>

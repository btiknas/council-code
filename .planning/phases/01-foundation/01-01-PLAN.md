---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/code-contrarian.md
  - agents/code-first-principles.md
  - agents/code-expansionist.md
  - agents/code-outsider.md
  - agents/code-executor.md
  - skills/council-code/SKILL.md
autonomous: true
requirements: [FOUND-01, FOUND-03, FOUND-04]

must_haves:
  truths:
    - "All 5 agent files carry code- prefix in agents/ directory"
    - "No bare-name agent files remain in agents/ directory"
    - "SKILL.md references code-prefixed agent paths, not bare names"
    - "SKILL.md has disable-model-invocation: true in frontmatter"
    - "SKILL.md name: field is still council-code (triggers preserved)"
  artifacts:
    - path: "agents/code-contrarian.md"
      provides: "Renamed contrarian agent"
      contains: "name: code-contrarian"
    - path: "agents/code-executor.md"
      provides: "Renamed executor agent"
      contains: "name: code-executor"
    - path: "skills/council-code/SKILL.md"
      provides: "Updated orchestrator with new agent refs and frontmatter"
      contains: "disable-model-invocation: true"
  key_links:
    - from: "skills/council-code/SKILL.md"
      to: "agents/code-contrarian.md"
      via: "reference path in References section"
      pattern: "agents/code-contrarian.md"
    - from: "skills/council-code/SKILL.md"
      to: "agents/code-executor.md"
      via: "reference path in References section"
      pattern: "agents/code-executor.md"
---

<objective>
Rename all 5 agent files to code-prefixed names and update SKILL.md with new references and disable-model-invocation frontmatter.

Purpose: Establish the code- namespace prefix so future councils (strategy, design, etc.) can add agents without naming collisions. Add invocation control frontmatter per FOUND-03.
Output: 5 renamed agent files, updated SKILL.md with code-prefixed references and disable-model-invocation: true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md

<interfaces>
<!-- Current agent frontmatter pattern (same for all 5 agents): -->
From agents/contrarian.md (lines 1-5):
```yaml
---
name: contrarian
description: Devil's advocate for code decisions. Finds the fatal flaw, challenges consensus, surfaces hidden failure modes in proposed architectures, libraries, APIs, refactors, or algorithms.
tools: Read, Grep, Glob, WebSearch, WebFetch
---
```

<!-- Current SKILL.md frontmatter (lines 1-4): -->
From skills/council-code/SKILL.md:
```yaml
---
name: council-code
description: Multi-perspective code decision council. Runs 5 expert advisors (Contrarian, First Principles, Expansionist, Outsider, Executor) in parallel on a code/architecture/engineering question, then synthesizes a chairman verdict with agreements, clashes, blind spots, and a concrete next step.
---
```

<!-- SKILL.md agent references to update (lines 40, 108-112): -->
Line 40: `Agent definitions live in agents/ at the repo root (see agents/contrarian.md, etc.).`
Lines 108-112:
```
- agents/contrarian.md -- Fatal flaw finder
- agents/first-principles.md -- Primitives reducer
- agents/expansionist.md -- Upside seeker
- agents/outsider.md -- Distant-domain importer
- agents/executor.md -- Monday-morning shipper
```

<!-- Full rename mapping: -->
| Old filename           | New filename                 | Old name:          | New name:                |
|------------------------|------------------------------|--------------------|--------------------------|
| contrarian.md          | code-contrarian.md           | contrarian         | code-contrarian          |
| first-principles.md    | code-first-principles.md     | first-principles   | code-first-principles    |
| expansionist.md        | code-expansionist.md         | expansionist       | code-expansionist        |
| outsider.md            | code-outsider.md             | outsider           | code-outsider            |
| executor.md            | code-executor.md             | executor           | code-executor            |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename 5 agent files with code- prefix and update name: field</name>
  <files>agents/code-contrarian.md, agents/code-first-principles.md, agents/code-expansionist.md, agents/code-outsider.md, agents/code-executor.md</files>
  <read_first>agents/contrarian.md, agents/first-principles.md, agents/expansionist.md, agents/outsider.md, agents/executor.md</read_first>
  <action>
For each of the 5 agent files, perform a git mv to rename with code- prefix, then update the `name:` field in YAML frontmatter. The body content and all other frontmatter fields (`description:`, `tools:`) remain UNCHANGED.

Concrete steps for each file:
1. `git mv agents/contrarian.md agents/code-contrarian.md` -- then edit line 2: `name: contrarian` becomes `name: code-contrarian`
2. `git mv agents/first-principles.md agents/code-first-principles.md` -- then edit line 2: `name: first-principles` becomes `name: code-first-principles`
3. `git mv agents/expansionist.md agents/code-expansionist.md` -- then edit line 2: `name: expansionist` becomes `name: code-expansionist`
4. `git mv agents/outsider.md agents/code-outsider.md` -- then edit line 2: `name: outsider` becomes `name: code-outsider`
5. `git mv agents/executor.md agents/code-executor.md` -- then edit line 2: `name: executor` becomes `name: code-executor`

Per D-01: Hard cutover. No compatibility shims, no dual names. The old bare-name files must not exist after this task.

Do NOT change description text, tools list, or any body content. Only the filename and name: field value change.
  </action>
  <verify>
    <automated>cd /Users/D052192/src/council-code && ls agents/code-contrarian.md agents/code-first-principles.md agents/code-expansionist.md agents/code-outsider.md agents/code-executor.md && test ! -f agents/contrarian.md && test ! -f agents/first-principles.md && test ! -f agents/expansionist.md && test ! -f agents/outsider.md && test ! -f agents/executor.md && grep -q "name: code-contrarian" agents/code-contrarian.md && grep -q "name: code-executor" agents/code-executor.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/code-contrarian.md exists and contains `name: code-contrarian`
    - agents/code-first-principles.md exists and contains `name: code-first-principles`
    - agents/code-expansionist.md exists and contains `name: code-expansionist`
    - agents/code-outsider.md exists and contains `name: code-outsider`
    - agents/code-executor.md exists and contains `name: code-executor`
    - No file matching `agents/contrarian.md`, `agents/executor.md`, `agents/expansionist.md`, `agents/first-principles.md`, `agents/outsider.md` exists
    - Each file's `description:` and `tools:` fields are unchanged from the original
    - Each file's body content (everything after the closing `---`) is unchanged
  </acceptance_criteria>
  <done>All 5 agent files renamed to code-prefixed names with updated name: field; bare-name files no longer exist in agents/</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md frontmatter and agent references</name>
  <files>skills/council-code/SKILL.md</files>
  <read_first>skills/council-code/SKILL.md</read_first>
  <action>
Three changes to SKILL.md (per D-08, D-09, FOUND-03, FOUND-04):

1. **Add frontmatter key** (per D-09 / FOUND-03): Insert `disable-model-invocation: true` as a new line between the `description:` line and the closing `---`. The frontmatter becomes:
```yaml
---
name: council-code
description: Multi-perspective code decision council. Runs 5 expert advisors (Contrarian, First Principles, Expansionist, Outsider, Executor) in parallel on a code/architecture/engineering question, then synthesizes a chairman verdict with agreements, clashes, blind spots, and a concrete next step.
disable-model-invocation: true
---
```
Do NOT change `name: council-code` -- this preserves `/council-code` and `/council` triggers (FOUND-04).

2. **Update line 40 body text** (per D-08): Change:
`Agent definitions live in agents/ at the repo root (see agents/contrarian.md, etc.).`
to:
`Agent definitions live in agents/ at the repo root (see agents/code-contrarian.md, etc.).`

3. **Update References section lines 108-112** (per D-08): Change each agent path from bare to code-prefixed:
```
- `agents/code-contrarian.md` -- Fatal flaw finder
- `agents/code-first-principles.md` -- Primitives reducer
- `agents/code-expansionist.md` -- Upside seeker
- `agents/code-outsider.md` -- Distant-domain importer
- `agents/code-executor.md` -- Monday-morning shipper
```

4. **Grep sweep**: After edits, run `grep -n 'agents/contrarian\|agents/first-principles\|agents/expansionist\|agents/outsider\|agents/executor' skills/council-code/SKILL.md` to catch any remaining bare-name references. Also check Step 2 protocol text around line 66 for `agents/<role>.md` pattern references and update to `agents/code-<role>.md` if found.
  </action>
  <verify>
    <automated>cd /Users/D052192/src/council-code && grep -q "disable-model-invocation: true" skills/council-code/SKILL.md && grep -q "name: council-code" skills/council-code/SKILL.md && grep -q "agents/code-contrarian.md" skills/council-code/SKILL.md && grep -q "agents/code-executor.md" skills/council-code/SKILL.md && ! grep -q "agents/contrarian\.md\|agents/first-principles\.md\|agents/expansionist\.md\|agents/outsider\.md\|agents/executor\.md" skills/council-code/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md frontmatter contains `disable-model-invocation: true` between `description:` and closing `---`
    - SKILL.md frontmatter still contains `name: council-code` (unchanged)
    - Line 40 references `agents/code-contrarian.md` not `agents/contrarian.md`
    - References section lists all 5 agents with `code-` prefix: `agents/code-contrarian.md`, `agents/code-first-principles.md`, `agents/code-expansionist.md`, `agents/code-outsider.md`, `agents/code-executor.md`
    - Zero grep matches for bare agent names (`agents/contrarian.md`, `agents/executor.md`, etc.) in the entire SKILL.md file
  </acceptance_criteria>
  <done>SKILL.md has disable-model-invocation frontmatter, all agent references are code-prefixed, and /council-code + /council triggers are preserved via unchanged name: field</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo files -> ~/.claude/agents/ | Agent files are symlinked/copied to user's Claude config directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | agents/*.md frontmatter | accept | Agent files are local Markdown; no untrusted input; user controls repo |
| T-01-02 | Denial of Service | SKILL.md broken frontmatter | mitigate | Verify YAML frontmatter is well-formed (grep for opening and closing `---` delimiters and key presence) |
</threat_model>

<verification>
1. `ls agents/code-*.md` returns exactly 5 files
2. `ls agents/ | grep -v '^code-'` returns nothing (no bare-name files)
3. `grep "disable-model-invocation: true" skills/council-code/SKILL.md` matches
4. `grep "name: council-code" skills/council-code/SKILL.md` matches (trigger preserved)
5. `grep -c "agents/code-" skills/council-code/SKILL.md` returns 6 (1 body + 5 references)
</verification>

<success_criteria>
- All 5 agent files exist at agents/code-{role}.md with updated name: field
- No bare-name agent files remain in agents/
- SKILL.md has disable-model-invocation: true in frontmatter
- SKILL.md references only code-prefixed agent paths
- name: council-code is unchanged (backward-compatible triggers)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

# mcp-server/

Reserved for a future MCP server that could expose the council as an MCP tool (usable from Claude Desktop, other MCP-capable clients, etc.).

Currently empty — the skill itself is pure Markdown and needs no server.
